/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'shorturl.at',
			'storage.googleapis.com',
			'www.amazon.com',
			'pagedone.io',
			'media.graphassets.com',
		],
	},
};

export default nextConfig;
